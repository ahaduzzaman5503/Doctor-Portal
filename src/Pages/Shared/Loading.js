import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn btn-success text-xl loading container mx-auto flex justify-center items-center w-48 mt-40">loading</button>
        </div>
    );
};

export default Loading;